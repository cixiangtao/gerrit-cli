{
  "name": "@anys/gerrit-cli",
  "version": "0.2.0",
  "description": "A safe local Git workflow CLI for Gerrit Code Review.",
  "keywords": [
    "cli",
    "code-review",
    "gerrit",
    "git",
    "workflow"
  ],
  "homepage": "https://github.com/cixiangtao/gerrit-cli",
  "bugs": {
    "url": "https://github.com/cixiangtao/gerrit-cli/issues"
  },
  "license": "MIT",
  "author": "cixiangtao",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cixiangtao/gerrit-cli.git"
  },
  "bin": {
    "gerrit": "dist/cli.js"
  },
  "files": [
    "dist",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "audit:prod": "pnpm audit --prod --audit-level high",
    "build": "tsc -p tsconfig.build.json && node scripts/finalize-build.mjs",
    "check": "pnpm lint && pnpm type-check && pnpm test",
    "dev": "tsx src/cli.ts",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "lint": "oxlint . && pnpm format:check",
    "lint:fix": "oxlint . --fix && pnpm format",
    "prepack": "pnpm build",
    "pretest": "pnpm build",
    "release:check": "pnpm check && pnpm audit:prod && pnpm verify:package",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "verify:package": "pnpm build && node scripts/verify-package.mjs"
  },
  "dependencies": {
    "@clack/prompts": "0.11.0",
    "commander": "^13.1.0",
    "picocolors": "^1.1.1",
    "string-width": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "packageManager": "pnpm@10.34.5"
}
